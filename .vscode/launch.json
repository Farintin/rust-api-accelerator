{
  "configurations": [
    {
      "name": "Containers: Node.js Launch",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug",
      "platform": "node",
      "node": {
        "package": "${workspaceFolder}/node-baseline-api/package.json",
        "localRoot": "${workspaceFolder}/node-baseline-api"
      }
    }
  ]
}
